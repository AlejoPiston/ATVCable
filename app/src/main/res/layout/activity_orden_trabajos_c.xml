<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_gradiente"
    tools:context=".OrdenTrabajosCActivity">
    <TextView
        android:layout_marginTop="12dp"
        android:text="Órdenes de trabajo confirmadas"
        android:textColor="@color/white"
        style="@style/Base.TextAppearance.AppCompat.Medium"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <LinearLayout
        android:orientation="vertical"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:layout_width="match_parent"
            android:orientation="horizontal"
            android:padding="10dp"
            android:layout_height="wrap_content"
            android:background="@color/ubicacion">
            <TextView
                android:layout_width="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/white"
                android:background="@color/ubicacion"
                android:id="@+id/txtLat"
                android:layout_height="wrap_content"
                tools:text="Latitud"/>
            <TextView
                android:layout_width="wrap_content"
                android:textColor="@color/white"
                android:background="@color/ubicacion"
                android:layout_height="wrap_content"
                android:id="@+id/txtLong"
                tools:text="Longitud"/>
        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/ubicacion"
            android:textColor="@color/white"
            android:gravity="center_horizontal"
            android:drawableLeft="@drawable/ic_ubicacion"
            android:paddingHorizontal="6dp"
            style="@style/Base.TextAppearance.AppCompat.Medium"
            android:id="@+id/txtDireccion"
            tools:text="Dirección"/>
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:id="@+id/txtTime"
            android:textColor="@color/white"
            android:background="@color/ubicacion"
            tools:text="Tiempo"/>
    </LinearLayout>


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerviewOrdenTrabajosC"
        tools:listitem="@layout/item_ordentrabajoc"
        android:padding="6dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </androidx.recyclerview.widget.RecyclerView>

</LinearLayout>